<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>AirlineReservationSystem</display-name>

    <!-- ── Welcome file ─────────────────────────────────────── -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- ── Session timeout: 30 minutes ─────────────────────── -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!--
        NOTE: Servlet mappings are handled via @WebServlet annotations.
        No additional <servlet> / <servlet-mapping> entries are needed.

        Servlets registered via annotations:
          /register       → RegisterServlet
          /login          → LoginServlet
          /logout         → LogoutServlet
          /searchFlight   → SearchFlightServlet
          /bookFlight     → BookFlightServlet
          /bookings       → ViewBookingsServlet
          /cancelBooking  → CancelBookingServlet
    -->

</web-app>
